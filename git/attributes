# use npm-merge-driver for merging and hide from diffs
npm-shrinkwrap.json merge=npm-merge-driver -diff
package-lock.json merge=npm-merge-driver -diff

# handle line endings correctly on Windows
* text=auto eol=lf
*.{cmd,[cC][mM][dD]} text eol=crlf
*.{bat,[bB][aA][tT]} text eol=crlf
