#!/usr/bin/env bash

set -euo pipefail

# Work around playlist caching bug
rm -f "${XDG_CACHE_HOME:-$HOME/.cache}"/ncspot/playlists.db

exec kitty \
	--class ncspot \
	--override 'font_family=Roboto Medium' \
	--override 'font_size=12' \
	--override 'adjust_line_height=125%' \
	ncspot
