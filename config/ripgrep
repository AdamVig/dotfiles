# Search case-insensitively if pattern is all lowercase
--smart-case

# Include hidden files
--hidden

# Always ignore Git directory
--glob
!.git/

# Always ignore node_modules
--glob
!node_modules/

# Always ignore package-lock.json
--glob
!package-lock.json

# Only show a preview instead of outputting long lines in full
--max-columns-preview
--max-columns
150

# Add a type for test files
--type-add
test:*.spec.*
--type-add
test:*.test.*

# Add a type for Storybook files
--type-add
story:*.stories.*